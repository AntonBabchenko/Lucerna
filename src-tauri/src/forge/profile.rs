//! Helpers for assembling the merged `VersionDetails` returned by
//! installer handlers. Phase 1 uses only `assemble_from_legacy`;
//! transitional/modern reuse the helpers in later phases.

// Filled in by Task 12.
