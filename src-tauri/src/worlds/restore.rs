//! Restore-side operations: `restore_backup` in both Replace and
//! AsCopy modes. Replace mode includes the auto-pre-restore safety
//! zip and atomic-ish tmp-rename flow. Implementation in Task 6.
