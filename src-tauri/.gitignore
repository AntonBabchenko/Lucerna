# Generated by Cargo
# will have compiled files and executables
/target/
# Ad-hoc isolated target dirs — see the root `.gitignore` for why they exist.
/target-*/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# CycloneDX SBOM. `cargo cyclonedx --manifest-path src-tauri/Cargo.toml`
# writes `src-tauri/<crate>.cdx.json` into the working tree; the release
# workflow copies it into `dist/` and publishes only that copy.
/*.cdx.json
